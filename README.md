# Bitonic Sort written in Rust

This repository is a sample implementation of bitonic sort described in "実践 Rust 入門".

## Test

```bash
$ cargo test
```

## Benchmark

```bash
$ cargo run --release --example benchmark -- 26
```
